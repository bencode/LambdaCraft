# dependencies
node_modules/

# build output
dist/
.astro/

# env / secrets — wx app uses .env for WeChat APP_ID/APP_SECRET
.env
.env.*

# logs (pnpm / npm error logs)
*.log

# review sidecars — local review notes, not for commit
apps/web/.reviews/
